<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sessp.com.sessp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CnpjActivity" />
        <activity android:name=".ConveniadoActivity" />
        <activity android:name=".MunicipioActivity" />
        <activity android:name=".DrsActivity" />
        <activity android:name=".ProgramaActivity" />
        <activity android:name=".PalavraChaveActivity" />
        <activity android:name=".NaturezaActivity" />
        <activity
            android:name=".ResultadoActivity"
            android:label="@string/title_activity_resultado"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>